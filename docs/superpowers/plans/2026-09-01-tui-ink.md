# TUI de xonecode con Ink — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Una piel TUI a pantalla completa (Ink/React) para la consola de xonecode: transcript con scroll, input con borde, sidebar de contexto y modal de aprobación — con la consola stdio intacta como fallback.

**Architecture:** La TUI es otra implementación de `Piel` (`core/turno.ts`) y de `Consola` (`cli/consola.ts`), montada con Ink en `src/cli/tui/`. El estado de presentación vive en un store TypeScript puro (sin React) que los componentes pintan; la lógica de streaming/fases se prueba ahí. Ink y react solo pueden importarse bajo `src/cli/tui/` (guardián nuevo).

**Tech Stack:** ink@^5, react@^18, ink-testing-library@^4, @types/react@^18. Sin más dependencias de UI.

**Spec:** `docs/superpowers/specs/2026-09-01-tui-ink-design.md`

## Global Constraints

- **Ink/react SOLO bajo `src/cli/tui/`.** El test guardián nuevo lo recorre y falla fuera.
- **`npm test` sin clave, conexión ni simulador** — el invariante del repo. Las pruebas de componentes corren con dobles, nunca contra Ollama.
- **La salida stdio NO cambia**: el e2e de guion en pipe (`printf 'hola\n/salir\n' | ./bin/xonecode --guion`) debe seguir byte-idéntico al final del plan. Ninguna tarea modifica `cli/stdio.ts` ni `cli/tema.ts` salvo lecturas.
- TDD estricto en cada tarea: test primero, ver el fallo, mínimo verde.
- Los tests NO pueden ser TTY: los componentes se prueban con `ink-testing-library`, la lógica pura sin React.
- Commits por tarea, en español, con la convención del repo.

**User decisions (already made):**
- Motor: Ink/React (reaprueba lo que el Camino A había excluido).
- Convivencia: TUI por omisión con TTY real; pipe/CI sigue en stdio; `--no-tui` fuerza stdio, `--tui` fuerza la TUI.
- Alcance v1: núcleo + sidebar (input con borde, transcript markdown, fases con duración, tools compactas, modal de aprobación fail-closed, sidebar de contexto/modelos/ruta/versión).
- Tema: dark-first con los azules de XOne; sin temas de usuario.
- Ratón: fuera de v1.

---

### Task 1: Infraestructura Ink y guardián de frontera

**Goal:** Las dependencias instaladas y un test que garantiza que ink/react no se importan fuera de `src/cli/tui/`.

**Files:**
- Modify: `package.json` (deps)
- Create: `src/cli/tui/frontera.test.ts`

**Acceptance Criteria:**
- [ ] `npm i` resuelve ink/react/@types/react/ink-testing-library.
- [ ] El test guardián pasa con el árbol actual (no hay imports aún) y falla si se añade un import de ink o react fuera de `cli/tui/`.
- [ ] Suite completa verde.

**Verify:** `npm test` → 0 fallos; el guardián lista `frontera.test.ts` entre sus propias exclusiones.

**Steps:**

- [ ] **Step 1: Instalar dependencias**

```bash
npm i ink@^5.2.1 react@^18.3.1 ink-testing-library@^4.0.0
npm i -D @types/react@^18.3.12
```

- [ ] **Step 2: El guardián (RED)**

`src/cli/tui/frontera.test.ts` — mismo estilo que el guardián de `tema.test.ts`:

```typescript
/**
 * La frontera de la TUI: ink y react viven SOLO aquí dentro.
 *
 * La piel stdio y todo el resto de la consola no pueden saber que existe una TUI —
 * igual que `core/imports.test.ts` prohíbe langchain en `core/`. Una TUI es una piel
 * más; si un import de ink se cuela fuera de `cli/tui/`, el fallback de pipes deja de
 * serlo.
 */
import { describe, it, expect } from "vitest";
import { readdirSync, readFileSync, statSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";

function ficherosTs(carpeta: string): string[] {
  const fuera: string[] = [];
  for (const nombre of readdirSync(carpeta)) {
    const ruta = join(carpeta, nombre);
    if (statSync(ruta).isDirectory()) {
      if (nombre === "__oro__" || nombre === "node_modules" || nombre === "dist") continue;
      fuera.push(...ficherosTs(ruta));
    } else if (nombre.endsWith(".ts") || nombre.endsWith(".tsx")) {
      if (nombre.endsWith(".test.ts") || nombre.endsWith(".test.tsx")) continue;
      fuera.push(ruta);
    }
  }
  return fuera;
}

it("ink y react solo se importan bajo cli/tui/", () => {
  const raiz = join(dirname(fileURLToPath(import.meta.url)), "..", "..");
  const casaTui = join(raiz, "src", "cli", "tui");
  const culpables: string[] = [];
  for (const ruta of ficherosTs(join(raiz, "src"))) {
    if (ruta.startsWith(casaTui)) continue;
    const fuente = readFileSync(ruta, "utf8");
    if (/(from\s+")ink|from\s+"react|from\s+"ink-testing-library/.test(fuente)) {
      culpables.push(ruta);
    }
  }
  expect(culpables, `imports de TUI fuera de cli/tui/: ${culpables.join(", ")}`).toEqual([]);
});
```

- [ ] **Step 3: Verificar GREEN** — `npx vitest run src/cli/tui/frontera.test.ts` → pasa (aún no hay imports). Después, `npm test` completo.
- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json src/cli/tui/frontera.test.ts
git commit -m "TUI: dependencias Ink y guardián de frontera cli/tui/"
```

---

### Task 2: Segmentos markdown puros en `cli/markdown.ts`

**Goal:** Una función pura que parte una línea de markdown en segmentos estilizados, para que la TUI la pinte con Ink (y los tests la prueben sin React).

**Files:**
- Modify: `src/cli/markdown.ts` (añadir export, no tocar los existentes)
- Test: `src/cli/markdown.test.ts`

**Acceptance Criteria:**
- [ ] `segmentosDe("la **Hola** está \`lista\`")` → exactamente los segmentos normal/negrita/mudo.
- [ ] Texto sin marcadores → un segmento normal. Marcadores sin cerrar → literal, nunca partido.
- [ ] Los 14 tests existentes de markdown siguen en verde.

**Verify:** `npx vitest run src/cli/markdown.test.ts` → todas en verde.

**Steps:**

- [ ] **Step 1: RED** — añadir a `src/cli/markdown.test.ts`:

```typescript
describe("segmentosDe", () => {
  it("parte una línea en segmentos normal, negrita y mudo", () => {
    expect(segmentosDe("la **Hola** está `lista`")).toEqual([
      { texto: "la ", estilo: "normal" },
      { texto: "Hola", estilo: "negrita" },
      { texto: " está ", estilo: "normal" },
      { texto: "lista", estilo: "mudo" },
    ]);
  });

  it("texto sin marcadores: un segmento normal", () => {
    expect(segmentosDe("hola tal cual")).toEqual([{ texto: "hola tal cual", estilo: "normal" }]);
  });

  it("un marcador sin cerrar queda LITERAL, nunca partido", () => {
    expect(segmentosDe("esto es **a medio")).toEqual([{ texto: "esto es **a medio", estilo: "normal" }]);
  });
});
```

Con import actualizado: `import { RenderizadorDeMarkdown, puntoSeguro, segmentosDe } from "./markdown.js";`

- [ ] **Step 2: Verificar RED** — `npx vitest run src/cli/markdown.test.ts` → falla porque `segmentosDe` no existe.
- [ ] **Step 3: GREEN** — añadir a `src/cli/markdown.ts`:

```typescript
/** Un trozo de línea con su significado: la TUI lo pinta y la stdio lo colorea. */
export type EstiloDeSegmento = "normal" | "negrita" | "mudo";
export interface Segmento {
  texto: string;
  estilo: EstiloDeSegmento;
}

/**
 * Parte una línea en segmentos `**negrita**` y `` `mudo` `` — la MISMA gramática que
 * `renderizarInline`, pero en datos y no en ANSI: la TUI no puede usar códigos de
 * escape dentro de un `<Text>` de Ink, así que pide significado y no color.
 */
export function segmentosDe(texto: string): Segmento[] {
  const segmentos: Segmento[] = [];
  const re = /\*\*([^*\n]+)\*\*|`([^`\n]+)`/g;
  let ultimo = 0;
  let m: RegExpExecArray | null;
  while ((m = re.exec(texto)) !== null) {
    if (m.index > ultimo) segmentos.push({ texto: texto.slice(ultimo, m.index), estilo: "normal" });
    segmentos.push(
      m[1] !== undefined
        ? { texto: m[1]!, estilo: "negrita" }
        : { texto: m[2]!, estilo: "mudo" }
    );
    ultimo = m.index + m[0].length;
  }
  if (ultimo < texto.length) segmentos.push({ texto: texto.slice(ultimo), estilo: "normal" });
  return segmentos.length > 0 ? segmentos : [{ texto, estilo: "normal" }];
}
```

- [ ] **Step 4: Verificar GREEN** — `npx vitest run src/cli/markdown.test.ts` → 17 tests en verde.
- [ ] **Step 5: Commit**

```bash
git add src/cli/markdown.ts src/cli/markdown.test.ts
git commit -m "TUI: segmentosDe — markdown en datos para la piel de Ink"
```

---

### Task 3: El store puro y la piel TUI

**Goal:** El estado de presentación de la TUI como TypeScript puro (`store.ts`) y su envoltorio `Piel` (`pielTui.ts`), con la semántica de streaming/fases ya probada en stdio.

**Files:**
- Create: `src/cli/tui/store.ts`
- Create: `src/cli/tui/pielTui.ts`
- Test: `src/cli/tui/store.test.ts`

**Acceptance Criteria:**
- [ ] Los tokens fluyen a un colchón; una línea confirmada se finaliza como acto `asistente` con su texto RAW (el render markdown es del componente).
- [ ] Una fase abre una fase activa con su `t0`; el acto que la desplaza cierra la fase como acto `fase` con los ms transcurridos.
- [ ] `fin(ms)` añade el acto `fin`; `pausa()` marca que hay una aprobación esperando; `usuario(texto)` y `error(texto)` añaden sus actos.
- [ ] `pielTui()` implementa la interfaz `Piel` completa (token, cerrarLinea, linea, pausa, fin, fase, notificacion) delegando en el store.
- [ ] Suscribirse (`suscribir`) recibe un aviso en cada mutación.

**Verify:** `npx vitest run src/cli/tui/store.test.ts` → verde; `npm test` completo → verde.

**Steps:**

- [ ] **Step 1: RED — tests del store**

`src/cli/tui/store.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { crearStore } from "./store.js";

describe("el store de la TUI", () => {
  it("los tokens esperan al salto de línea y la línea confirmada es un acto asistente", () => {
    const s = crearStore();
    s.token("Hola. ¿En qué te ayudo");
    expect(s.estado().actos).toEqual([]);
    s.token(" con xonecode?\nHecho.");
    expect(s.estado().actos).toEqual([{ tipo: "asistente", texto: "Hola. ¿En qué te ayudo con xonecode?" }]);
    expect(s.estado().colchon).toBe("Hecho.");
    s.cerrarLinea();
    expect(s.estado().actos).toEqual([
      { tipo: "asistente", texto: "Hola. ¿En qué te ayudo con xonecode?" },
      { tipo: "asistente", texto: "Hecho." },
    ]);
    expect(s.estado().colchon).toBe("");
  });

  it("la fase se abre viva y el acto que la desplaza la cierra con su duración", () => {
    let ahora = 1000;
    const s = crearStore({ ahora: () => ahora });
    s.fase("planificando");
    expect(s.estado().faseActiva).toEqual({ texto: "planificando", t0: 1000 });
    ahora = 1600;
    s.linea("→ lee app.xne");
    expect(s.estado().faseActiva).toBeUndefined();
    expect(s.estado().actos[0]).toEqual({ tipo: "fase", texto: "planificando", ms: 600 });
    expect(s.estado().actos[1]).toEqual({ tipo: "tool", texto: "→ lee app.xne" });
  });

  it("usuario, error y fin son actos propios", () => {
    const s = crearStore();
    s.usuario("hola");
    s.error("Error: se cayó");
    s.fin(2400);
    expect(s.estado().actos).toEqual([
      { tipo: "usuario", texto: "hola" },
      { tipo: "error", texto: "Error: se cayó" },
      { tipo: "fin", ms: 2400 },
    ]);
  });

  it("pausa marca la aprobación pendiente y el suscriptor se entera de cada mutación", () => {
    const s = crearStore();
    const avisos: number[] = [];
    s.suscribir(() => avisos.push(avisos.length + 1));
    s.pausa();
    expect(s.estado().aprobacionPendiente).toBe(true);
    expect(avisos).toEqual([1]);
  });
});
```

- [ ] **Step 2: Verificar RED** — falla: no existe `./store.js`.
- [ ] **Step 3: GREEN — `store.ts`**

```typescript
/**
 * El estado de presentación de la TUI, SIN React.
 *
 * Los componentes solo pintan; la semántica (qué cierra una fase, cuándo una línea
 * confirmada es definitiva) vive aquí y se prueba sin montar nada. Es el mismo pacto
 * que la piel stdio —tokens a colchón, cascada de fases— pero el destino es el estado,
 * no stdout: en TUI el repintado es total y no hay append-only que respetar.
 */

/** Un acto del transcript: lo que ya no cambia y se pinta por su tipo. */
export type Acto =
  | { tipo: "usuario"; texto: string }
  | { tipo: "asistente"; texto: string }
  | { tipo: "tool"; texto: string }
  | { tipo: "sistema"; texto: string }
  | { tipo: "fase"; texto: string; ms: number }
  | { tipo: "fin"; ms: number }
  | { tipo: "error"; texto: string };

export interface EstadoDeTui {
  actos: Acto[];
  /** El streaming en curso: la frase a medio confirmar. */
  colchon: string;
  faseActiva?: { texto: string; t0: number };
  aprobacionPendiente: boolean;
}

export interface OpcionesDelStore {
  /** Costura de test del reloj: la duración de una fase no puede dormir el test. */
  ahora?: () => number;
}

export function crearStore(opciones: OpcionesDelStore = {}) {
  const ahora = opciones.ahora ?? Date.now;
  let estado: EstadoDeTui = { actos: [], colchon: "", aprobacionPendiente: false };
  const suscriptores: (() => void)[] = [];

  const mutar = (cambio: Partial<EstadoDeTui>): void => {
    estado = { ...estado, ...cambio };
    for (const s of suscriptores) s();
  };

  /** La cascada, versión TUI: cualquier acto cierra la fase viva con su duración. */
  const cerrarFase = (): Acto[] => {
    if (estado.faseActiva === undefined) return [];
    const { texto, t0 } = estado.faseActiva;
    mutar({ faseActiva: undefined });
    return [{ tipo: "fase", texto, ms: Math.max(0, ahora() - t0) }];
  };

  const aniadir = (nuevos: Acto[]): void => {
    if (nuevos.length === 0) return;
    mutar({ actos: [...estado.actos, ...cerrarFase(), ...nuevos] });
  };

  return {
    estado: (): EstadoDeTui => estado,

    suscribir(suscriptor: () => void): void {
      suscriptores.push(suscriptor);
    },

    token(texto: string): void {
      let colchon = estado.colchon + texto;
      const confirmados: Acto[] = [];
      for (;;) {
        const salto = colchon.indexOf("\n");
        if (salto === -1) break;
        confirmados.push({ tipo: "asistente", texto: colchon.slice(0, salto) });
        colchon = colchon.slice(salto + 1);
      }
      mutar({
        colchon,
        ...(confirmados.length > 0 ? { actos: [...estado.actos, ...cerrarFase(), ...confirmados] } : {}),
      });
    },

    cerrarLinea(): void {
      if (estado.colchon === "") return;
      aniadir([{ tipo: "asistente", texto: estado.colchon }]);
      mutar({ colchon: "" });
    },

    /** Línea de sistema/tool/aviso: acto inmediato, texto ya finalizado. */
    linea(texto: string, tipo: Acto["tipo"] = "tool"): void {
      aniadir([{ tipo, texto }]);
    },

    fase(texto: string): void {
      mutar({ faseActiva: { texto, t0: ahora() } });
    },

    pausa(): void {
      mutar({ aprobacionPendiente: true });
    },

    fin(ms: number): void {
      aniadir([{ tipo: "fin", ms }]);
    },
  };
}
```

- [ ] **Step 4: Verificar GREEN** — `npx vitest run src/cli/tui/store.test.ts`.
- [ ] **Step 5: pielTui.ts (con tests)** — `src/cli/tui/pielTui.ts`:

```typescript
/**
 * La piel TUI: implementa la interfaz `Piel` de `core/turno.ts` delegando en el store.
 * Es deliberadamente fina — la semántica ya vive en el store — y existe para que el
 * motor no sepa que hay una TUI.
 */
import type { Piel } from "../../core/turno.js";
import type { PendienteDeAprobacion } from "../../core/events.js";
import type { crearStore } from "./store.js";

type Store = ReturnType<typeof crearStore>;

export function crearPielTui(store: Store): Piel {
  return {
    token: (texto) => store.token(texto),
    cerrarLinea: () => store.cerrarLinea(),
    linea: (texto) => store.linea(texto),
    pausa: (_pendientes: PendienteDeAprobacion[]) => store.pausa(),
    fin: (ms) => store.fin(ms),
    fase: (texto) => store.fase(texto),
    // En TUI el panel de avisos no existe: el repintado es total y el aviso es una
    // línea de sistema más.
    notificacion: (texto) => store.linea(texto, "sistema"),
  };
}
```

Test en `store.test.ts` (o el mismo fichero): una piel TUI sobre el store reproduce la secuencia del motor: `piel.token("a")`, `piel.cerrarLinea()`, `piel.fase("planificando")`, `piel.linea("→ x")` → actos `[asistente a, fase planificando ms≥0, tool → x]`, y `piel.pausa([...])` marca pendiente.

- [ ] **Step 6: `npm test` completo → verde.**
- [ ] **Step 7: Commit**

```bash
git add src/cli/tui/store.ts src/cli/tui/pielTui.ts src/cli/tui/store.test.ts
git commit -m "TUI: store puro del transcript y piel de Ink"
```

---

### Task 4: `temaInk` — los tokens semánticos en estilos Ink

**Goal:** El vocabulario de color de la TUI: mismas claves semánticas que `cli/tema.ts`, valores hex de la paleta XOne.

**Files:**
- Create: `src/cli/tui/temaInk.ts`
- Test: `src/cli/tui/temaInk.test.ts`

**Acceptance Criteria:**
- [ ] Expone un objeto con claves `texto, mudo, negrita, exito, aviso, grave, anadido, quitado, acento, borde, fondoInput`.
- [ ] Todas las claves de `CON_COLOR` de `tema.ts` tienen espejo aquí (el test lo recorre).
- [ ] Paleta dark-first: azules de XOne en `acento`/`borde`.

**Verify:** `npx vitest run src/cli/tui/temaInk.test.ts` → verde.

**Steps:**

- [ ] **Step 1: RED**

```typescript
import { describe, it, expect } from "vitest";
import { temaInk } from "./temaInk.js";
import { crearTema } from "../tema.js";

describe("temaInk", () => {
  it("cada token semántico de tema.ts tiene espejo en la TUI", () => {
    for (const clave of Object.keys(crearTema(true))) {
      expect(temaInk, `token «${clave}»`).toHaveProperty(clave);
    }
  });

  it("los tokens son hex y dark-first: el acento y el borde son azules XOne", () => {
    expect(temaInk.acento).toMatch(/^#[0-9a-f]{6}$/i);
    expect(temaInk.borde).toMatch(/^#[0-9a-f]{6}$/i);
    expect(temaInk.acento.toLowerCase()).toBe("#38bdf8");
  });
});
```

- [ ] **Step 2: RED → GREEN** — `src/cli/tui/temaInk.ts`:

```typescript
/**
 * El vocabulario de color de la TUI: las MISMAS claves semánticas que `cli/tema.ts`
 * (`exito`, `grave`, `mudo`…), traducidas a la paleta de la TUI. La piel stdio pide
 * ANSI a tema.ts y la TUI pide hex a aquí — un solo significado, dos renderizadores.
 * Paleta dark-first con los azules de XOne.
 */
export const temaInk = {
  texto: "#d4d4d8",
  mudo: "#71717a",
  negrita: "#ffffff",
  exito: "#4ade80",
  aviso: "#fbbf24",
  grave: "#f87171",
  anadido: "#4ade80",
  quitado: "#f87171",
  acento: "#38bdf8",
  borde: "#27272a",
  fondoInput: "#18181b",
} as const;
```

- [ ] **Step 3: GREEN → commit**

```bash
git add src/cli/tui/temaInk.ts src/cli/tui/temaInk.test.ts
git commit -m "TUI: temaInk — los tokens semánticos en paleta XOne"
```

---

### Task 4b: el viewport del transcript

**Goal:** El componente que pinta los actos con scroll propio (PgUp/PgDn sobre una ventana de actos).

**Files:**
- Create: `src/cli/tui/transcript.tsx`
- Test: `src/cli/tui/transcript.test.tsx`

**Acceptance Criteria:**
- [ ] Muestra los ÚLTIMOS actos que caben en la altura dada (ventana desde el final).
- [ ] PgUp sube la ventana; PgDn baja; cualquier token nuevo la vuelve a anclar abajo (la ventana vive al fondo salvo que el usuario haya subido).
- [ ] Los actos se pintan por su tipo: `usuario` con `❯` y acento, `fase` con `+` y mudo + ms, `asistente` con markdown (cabecera `#`, viñeta, segmentos de `segmentosDe`), `tool`/`sistema` mudo, `error` grave, `fin` mudo `(2.4s)`.
- [ ] El colchón se pinta como línea en curso al fondo.

**Verify:** `npx vitest run src/cli/tui/transcript.test.tsx` → verde.

**Steps:**

- [ ] **Step 1: RED**

```tsx
import { describe, it, expect } from "vitest";
import { render } from "ink-testing-library";
import { Transcript } from "./transcript.js";
import { crearStore } from "./store.js";

describe("transcript", () => {
  it("pinta los últimos actos y el colchón como línea en curso", () => {
    const s = crearStore();
    s.usuario("hola");
    s.token("¡Hola! **listo**\n");
    const { lastFrame } = render(<Transcript store={s} altura={10} />);
    const salida = lastFrame() ?? "";
    expect(salida).toContain("❯ hola");
    expect(salida).toContain("¡Hola! listo");
    expect(salida).not.toContain("**");
  });

  it("con más actos que altura, la ventana vive al fondo", () => {
    const s = crearStore();
    for (let i = 0; i < 30; i++) s.linea(`línea ${i}`);
    const { lastFrame } = render(<Transcript store={s} altura={5} />);
    expect(lastFrame()).toContain("línea 29");
    expect(lastFrame()).not.toContain("línea 0\n");
  });
});
```

- [ ] **Step 2: GREEN — `transcript.tsx`**

```tsx
/**
 * El viewport del transcript: una ventana de actos con scroll PROPIO (PgUp/PgDn).
 *
 * La TUI es dueña del terminal, así que el scrollback del terminal ya no sirve: el
 * scroll es este componente. La ventana vive al fondo —donde pasa lo que importa— y
 * PgUp la sube; cualquier acto nuevo devuelve la ventana al fondo, porque leer lo
 * nuevo importa más que conservar la posición de lectura.
 */
import { Box, Text, useInput } from "ink";
import { useState, useEffect } from "react";
import type { ReactNode } from "react";
import { crearStore, type Acto, type EstadoDeTui } from "./store.js";
import { segmentosDe } from "../markdown.js";
import { temaInk } from "./temaInk.js";

type Store = ReturnType<typeof crearStore>;

/** Una línea de markdown a actos de Ink: cabecera, viñeta y segmentos inline. */
function LineaMarkdown({ texto }: { texto: string }): ReactNode {
  const cabecera = /^(#{1,3})\s+(.*)$/.exec(texto);
  if (cabecera) {
    return (
      <Text bold color={temaInk.negrita}>
        {cabecera[2]}
      </Text>
    );
  }
  const vineta = /^[-*]\s+(.*)$/.exec(texto);
  const cuerpo = vineta !== null ? vineta[1]! : texto;
  return (
    <Text>
      {vineta !== null ? <Text color={temaInk.acento}>{"• "}</Text> : null}
      {segmentosDe(cuerpo).map((seg, i) => (
        <Text
          key={i}
          bold={seg.estilo === "negrita"}
          color={seg.estilo === "negrita" ? temaInk.negrita : seg.estilo === "mudo" ? temaInk.mudo : undefined}
        >
          {seg.texto}
        </Text>
      ))}
    </Text>
  );
}

function ActoVista({ acto }: { acto: Acto }): ReactNode {
  switch (acto.tipo) {
    case "usuario":
      return (
        <Text>
          <Text color={temaInk.acento}>{"❯ "}</Text>
          {acto.texto}
        </Text>
      );
    case "asistente":
      return <LineaMarkdown texto={acto.texto} />;
    case "fase":
      return (
        <Text color={temaInk.mudo}>
          {`+ ${acto.texto}: ${(acto.ms / 1000).toFixed(1)}s`}
        </Text>
      );
    case "fin":
      return <Text color={temaInk.mudo}>{`(${(acto.ms / 1000).toFixed(1)}s)`}</Text>;
    case "error":
      return <Text color={temaInk.grave}>{acto.texto}</Text>;
    default:
      return <Text color={temaInk.mudo}>{`  ${acto.texto}`}</Text>;
  }
}

export function Transcript({ store, altura }: { store: Store; altura: number }): ReactNode {
  const [estado, setEstado] = useState<EstadoDeTui>(store.estado());
  const [desfase, setDesfase] = useState(0); // 0 = al fondo; N = N actos hacia arriba

  useEffect(() => {
    const quitar = () => setEstado({ ...store.estado() });
    store.suscribir(quitar);
    return () => {
      /* El store de la sesión vive tanto como la app; no hay nada que quitar. */
    };
  }, [store]);

  // Cualquier acto nuevo ancla la ventana al fondo.
  useEffect(() => setDesfase(0), [estado.actos.length]);

  useInput((_entrada, tecla) => {
    if (tecla.pageUp) setDesfase((d) => Math.min(estado.actos.length, d + 10));
    if (tecla.pageDown) setDesfase((d) => Math.max(0, d - 10));
  });

  const visibles = estado.actos.slice(Math.max(0, estado.actos.length - altura - desfase), estado.actos.length - desfase);
  return (
    <Box flexDirection="column">
      {visibles.map((acto, i) => (
        <ActoVista key={`${i}-${acto.tipo}`} acto={acto} />
      ))}
      {desfase === 0 && estado.colchon !== "" ? (
        <LineaMarkdown texto={estado.colchon} />
      ) : null}
    </Box>
  );
}
```

- [ ] **Step 3: GREEN → `npm test` → commit**

```bash
git add src/cli/tui/transcript.tsx src/cli/tui/transcript.test.tsx
git commit -m "TUI: viewport del transcript con scroll propio"
```

---

### Task 5: la entrada con borde

**Goal:** El cuadro de input: texto, historial ↑/↓, Tab que completa comandos y `@ficheros`, Enter que entrega la línea.

**Files:**
- Create: `src/cli/tui/entrada.tsx`
- Test: `src/cli/tui/entrada.test.tsx`

**Acceptance Criteria:**
- [ ] Escribe y borra; Enter llama a `alEnviar(linea)` y vacía el campo.
- [ ] ↑/↓ recorren el historial (lo más reciente primero; ↓ hasta la línea en edición).
- [ ] Tab: si la línea empieza por `/` propone comandos; si contiene `@` propone ficheros; con un único candidato lo completa; con varios muestra los primeros 8 como pista bajo el cuadro.
- [ ] `ocupado=true` deshabilita el envío y enseña el texto de turno en marcha.

**Verify:** `npx vitest run src/cli/tui/entrada.test.tsx` → verde.

**Steps:**

- [ ] **Step 1: RED**

```tsx
import { describe, it, expect } from "vitest";
import { render } from "ink-testing-library";
import { Entrada } from "./entrada.js";

/** Escribe una secuencia de teclas en el stdin falso de ink-testing-library. */
async function teclear(instancia: { stdin: { write: (d: string) => void } }, texto: string): Promise<void> {
  for (const letra of texto) {
    instancia.stdin.write(letra);
    await esperar();
  }
}
const esperar = (): Promise<void> => new Promise((r) => setTimeout(r, 10));

describe("entrada", () => {
  it("escribe, envía con Enter y vacía el campo", async () => {
    const enviadas: string[] = [];
    const instancia = render(<Entrada alEnviar={(l) => enviadas.push(l)} completa={() => [[], ""]} ocupado={false} historial={[]} />);
    await teclear(instancia, "hola");
    instancia.stdin.write("\r");
    await esperar();
    expect(enviadas).toEqual(["hola"]);
    expect(instancia.lastFrame()).not.toContain("hola\rhola");
  });

  it("Tab completa un comando único", async () => {
    const instancia = render(
      <Entrada
        alEnviar={() => {}}
        completa={(linea) => (linea === "/conf" ? [["/config"], linea] : [[], linea])}
        ocupado={false}
        historial={[]}
      />
    );
    await teclear(instancia, "/conf");
    instancia.stdin.write("\t");
    await esperar();
    expect(instancia.lastFrame()).toContain("/config");
  });
});
```

- [ ] **Step 2: GREEN — `entrada.tsx`**

```tsx
/**
 * El cuadro de entrada: el único sitio donde el usuario escribe.
 *
 * A mano y sin `ink-text-input`: el contrato es pequeño (texto, historial, Tab de
 * comandos y @ficheros reutilizando el completer puro de `consola.ts`) y una
 * dependencia más no lo acorta.
 */
import { Box, Text, useInput } from "ink";
import { useState } from "react";
import type { ReactNode } from "react";
import { temaInk } from "./temaInk.js";

export function Entrada({
  alEnviar,
  completa,
  ocupado,
  historial,
}: {
  alEnviar: (linea: string) => void;
  /** El completer puro de `consola.ts`: `(linea) => [candidatos, linea]`. */
  completa: (linea: string) => [string[], string];
  ocupado: boolean;
  historial: readonly string[];
}): ReactNode {
  const [valor, setValor] = useState("");
  const [indiceHistorial, setIndice] = useState(-1); // -1 = la línea en edición
  const [pista, setPista] = useState<string[]>([]);

  useInput(
    (entrada, tecla) => {
      if (tecla.return) {
        if (valor.trim() === "") return;
        alEnviar(valor);
        setValor("");
        setIndice(-1);
        setPista([]);
        return;
      }
      if (tecla.upArrow) {
        const siguiente = Math.min(historial.length - 1, indiceHistorial + 1);
        if (historial[siguiente] !== undefined) {
          setIndice(siguiente);
          setValor(historial[siguiente]!);
        }
        return;
      }
      if (tecla.downArrow) {
        const siguiente = indiceHistorial - 1;
        setIndice(siguiente);
        setValor(siguiente === -1 ? "" : historial[siguiente]!);
        return;
      }
      if (tecla.tab) {
        const [candidatos, base] = completa(valor);
        if (candidatos.length === 1) {
          setValor(candidatos[0]!);
          setPista([]);
        } else if (candidatos.length > 1) {
          setPista(candidatos.slice(0, 8).map((c) => c.replace(base, "")));
        }
        return;
      }
      if (tecla.backspace || tecla.delete) {
        setValor((v) => v.slice(0, -1));
        setPista([]);
        return;
      }
      if (entrada && !tecla.ctrl && !tecla.meta && !tecla.escape) {
        setValor((v) => v + entrada);
        setPista([]);
      }
    },
    { isActive: !ocupado }
  );

  return (
    <Box flexDirection="column" borderStyle="round" borderColor={temaInk.borde} paddingX={1}>
      {ocupado ? (
        <Text color={temaInk.mudo}>turno en curso… (Ctrl-C para cancelar el turno)</Text>
      ) : (
        <Text color={temaInk.texto}>
          {valor}
          <Text color={temaInk.acento}>{"▏"}</Text>
        </Text>
      )}
      {pista.length > 0 ? <Text color={temaInk.mudo}>{`  ${pista.join("   ")}`}</Text> : null}
    </Box>
  );
}
```

- [ ] **Step 3: `npm test` → commit**

```bash
git add src/cli/tui/entrada.tsx src/cli/tui/entrada.test.tsx
git commit -m "TUI: cuadro de entrada con historial y Tab de comandos y @ficheros"
```

---

### Task 6: sidebar y barra de estado

**Goal:** El panel derecho (contexto, modelos, proyecto) y la línea inferior de estado, como componentes puros que reciben sus datos.

**Files:**
- Create: `src/cli/tui/sidebar.tsx`
- Test: `src/cli/tui/sidebar.test.tsx`

**Acceptance Criteria:**
- [ ] Contexto: `ctx 25.7K/200K (13%)` — el porcentaje SOLO si hay tope (la regla de `core/contextos.ts`); sin contexto medido, la sección calla.
- [ ] Modelos por papel, proyecto + rama git, versión.
- [ ] La barra inferior: `modelo · ruta · /ayuda` en mudo.

**Verify:** `npx vitest run src/cli/tui/sidebar.test.tsx` → verde.

**Steps:**

- [ ] **Step 1: RED**

```tsx
import { describe, it, expect } from "vitest";
import { render } from "ink-testing-library";
import { Sidebar } from "./sidebar.js";

describe("sidebar", () => {
  it("enseña contexto con porcentaje SOLO si hay tope", () => {
    const { lastFrame } = render(
      <Sidebar contexto={25700} tope={200_000} modelo="ollama/glm" modelosPorPapel={{ rapido: "ollama/rapido" }} proyecto="MinitMT" rama="main" version="0.3.0" />
    );
    const salida = lastFrame() ?? "";
    expect(salida).toContain("13%");
    expect(salida).toContain("MinitMT");
    expect(salida).toContain("main");
  });

  it("sin tope no hay porcentaje — una cifra sobre un número inventado es una mentira", () => {
    const { lastFrame } = render(
      <Sidebar contexto={25700} tope={undefined} modelo="ollama/glm" modelosPorPapel={{}} proyecto="MinitMT" rama="main" version="0.3.0" />
    );
    expect(lastFrame() ?? "").not.toContain("%");
  });
});
```

- [ ] **Step 2: GREEN — `sidebar.tsx`**

```tsx
/**
 * La sidebar: lo que conviene tener delante sin pedirlo. Los datos entran por props —
 * quien compone la sesión los resuelve (tope por `topeResuelto`, rama por git) y este
 * componente solo pinta. El porcentaje SOLO con tope: la regla de `core/contextos.ts`.
 */
import { Box, Text } from "ink";
import type { ReactNode } from "react";
import { temaInk } from "./temaInk.js";
import type { Papel } from "../../core/ports.js";

const compacto = (n: number): string =>
  n < 1000 ? `${n}` : n >= 1_000_000 ? `${(n / 1_000_000).toFixed(1)}M` : `${(n / 1000).toFixed(1)}K`;

export interface DatosDeSidebar {
  contexto: number;
  tope?: number;
  modelo: string;
  modelosPorPapel: Partial<Record<Papel, string>>;
  proyecto: string;
  rama?: string;
  version: string;
}

export function Sidebar(d: DatosDeSidebar): ReactNode {
  return (
    <Box flexDirection="column" gap={1}>
      {d.contexto > 0 ? (
        <Box flexDirection="column">
          <Text bold color={temaInk.acento}>Context</Text>
          <Text>
            {compacto(d.contexto)}
            {d.tope !== undefined ? `/${compacto(d.tope)} (${Math.round((d.contexto / d.tope) * 100)}%)` : " tokens"}
          </Text>
        </Box>
      ) : null}
      <Box flexDirection="column">
        <Text bold color={temaInk.acento}>Modelo</Text>
        <Text>{d.modelo}</Text>
        {Object.entries(d.modelosPorPapel).map(([papel, m]) => (
          <Text key={papel} color={temaInk.mudo}>{`${papel}: ${m}`}</Text>
        ))}
      </Box>
      <Box flexDirection="column">
        <Text bold color={temaInk.acento}>Proyecto</Text>
        <Text>{d.proyecto}</Text>
        {d.rama !== undefined ? <Text color={temaInk.mudo}>{d.rama}</Text> : null}
        <Text color={temaInk.mudo}>xonecode {d.version}</Text>
      </Box>
    </Box>
  );
}
```

- [ ] **Step 3: `npm test` → commit**

```bash
git add src/cli/tui/sidebar.tsx src/cli/tui/sidebar.test.tsx
git commit -m "TUI: sidebar de contexto, modelos y proyecto"
```

---

### Task 7: el modal de aprobación

**Goal:** El reductor puro y el modal Ink que decide las aprobaciones dentro de la TUI, fail-closed, con el diff coloreado.

**Files:**
- Create: `src/cli/tui/aprobarTui.tsx`
- Test: `src/cli/tui/aprobarTui.test.tsx`

**Acceptance Criteria:**
- [ ] El reductor puro: con la lista de pendientes, `aprobar`/`rechazar` avanza el índice y acumula `Decision`; al agotar, `terminado()` devuelve el Map por id.
- [ ] El modal muestra pendiente actual (descripción, fichero, quién), el diff con `conContexto`/`recortar` (mismas constantes CONTEXTO_DEL_DIFF=2, TECHO_DEL_DIFF=25), y pinta `+ ` verde / `- ` rojo.
- [ ] `s`/`S` aprueba; `n`, Enter, Escape y Ctrl-C RECHAZAN (fail-closed).
- [ ] `pedirDecisionesTui(pendientes, ficheros, diffs, montar)` devuelve `Promise<Map<string, Decision>>` resuelta por el modal.

**Verify:** `npx vitest run src/cli/tui/aprobarTui.test.tsx` → verde.

**Steps:**

- [ ] **Step 1: RED — reductor puro**

```tsx
import { describe, it, expect } from "vitest";
import { reductorDeAprobacion, estadoInicial } from "./aprobarTui.js";
import type { PendienteDeAprobacion } from "../../core/events.js";

const pendiente = (id: string): PendienteDeAprobacion => ({
  id,
  origen: "dev",
  descripcion: "escribir Clientes.xne",
  decisionesPermitidas: ["approve", "reject"],
});

describe("el reductor de aprobación", () => {
  it("aprobar y rechazar acumulan decisiones y avanzan", () => {
    let s = estadoInicial([pendiente("i1"), pendiente("i2")]);
    s = reductorDeAprobacion(s, "aprobar");
    s = reductorDeAprobacion(s, "rechazar");
    expect(s.indice).toBe(2);
    expect(s.terminado!.get("i1")!.type).toBe("approve");
    expect(s.terminado!.get("i2")!.type).toBe("reject");
    // Idempotente al terminar: más decisiones después del agotado no cambian nada.
    const otro = reductorDeAprobacion(s, "aprobar");
    expect(otro.terminado).toBe(s.terminado);
  });

  it("agotar la lista SIN responder los restantes no puede dejarlos sin decisión (fail-closed)", () => {
    // La garantía real vive en `pedirDecisionesTui`: todo pendiente sin decisión
    // explícita sale RECHAZADO. Este test fija el contrato del reductor: al agotar,
    // `terminado` lleva una decisión POR CADA pendiente.
    let s = estadoInicial([pendiente("i1")]);
    s = reductorDeAprobacion(s, "aprobar");
    expect(s.terminado!.size).toBe(1);
    expect(s.terminado!.get("i1")!.type).toBe("approve");
  });
});
```

- [ ] **Step 2: GREEN — la lógica en `aprobarTui.tsx`** (reductor y tipo sin React al principio del fichero):

```typescript
import { interpretAnswer, type Decision } from "../../vendor/hitl.js";
import type { PendienteDeAprobacion } from "../../core/events.js";
import { conContexto, recortar, type LineaDeDiff } from "../../core/diff.js";
import { Box, Text, useInput } from "ink";
import { useState } from "react";
import type { ReactNode } from "react";
import { temaInk } from "./temaInk.js";

const CONTEXTO_DEL_DIFF = 2;
const TECHO_DEL_DIFF = 25;

export interface EstadoDeAprobacion {
  pendientes: PendienteDeAprobacion[];
  indice: number;
  decisiones: Map<string, Decision>;
  terminado?: Map<string, Decision>;
}

export function estadoInicial(pendientes: PendienteDeAprobacion[]): EstadoDeAprobacion {
  return { pendientes, indice: 0, decisiones: new Map() };
}

export function reductorDeAprobacion(s: EstadoDeAprobacion, respuesta: "aprobar" | "rechazar"): EstadoDeAprobacion {
  const p = s.pendientes[s.indice];
  if (p === undefined) return s;
  const decisiones = new Map(s.decisiones).set(p.id, { type: respuesta === "aprobar" ? "approve" : "reject" });
  const terminado = s.indice + 1 >= s.pendientes.length ? decisiones : undefined;
  return { ...s, indice: s.indice + 1, decisiones, terminado };
}

/**
 * El puerto `pedirAprobacion` en TUI. `montar` lo pone en el store de la app (el modal
 * se pinta); la promesa resuelve con TODAS las decisiones. **Desmontar el modal sin
 * responder es rechazo de los restantes** — la costura del fail-closed está aquí.
 */
export function pedirDecisionesTui(
  pendientes: PendienteDeAprobacion[],
  montar: (pendientes: PendienteDeAprobacion[], alTerminar: (d: Map<string, Decision>) => void) => void
): Promise<Map<string, Decision>> {
  return new Promise((resolver) => {
    if (pendientes.length === 0) return resolver(new Map());
    montar(pendientes, (decisiones) => {
      // Los pendientes sin decisión explícita: RECHAZO. Aprobar ejecuta; lo que no se
      // entiende no toca nada.
      for (const p of pendientes) {
        if (!decisiones.has(p.id)) decisiones.set(p.id, { type: "reject" });
      }
      resolver(decisiones);
    });
  });
}
```

Y el componente (mismo fichero):

```tsx
export function ModalAprobacion({
  pendientes,
  fichero,
  diff,
  alTerminar,
}: {
  pendientes: PendienteDeAprobacion[];
  fichero: (id: string) => string | undefined;
  diff: (id: string) => LineaDeDiff[] | undefined;
  alTerminar: (d: Map<string, Decision>) => void;
}): ReactNode {
  const [estado, setEstado] = useState(() => estadoInicial(pendientes));

  useInput((entrada, tecla) => {
    if (estado.terminado !== undefined) return;
    const esSi = entrada === "s" || entrada === "S";
    const esNo = entrada === "n" || entrada === "N" || tecla.return || tecla.escape || tecla.ctrl;
    if (!esSi && !esNo) return;
    const siguiente = reductorDeAprobacion(estado, esSi ? "aprobar" : "rechazar");
    setEstado(siguiente);
    if (siguiente.terminado !== undefined) alTerminar(siguiente.terminado);
  });

  const p = estado.pendientes[estado.indice];
  if (p === undefined) return null;
  const lineas = diff(p.id);
  const visibles = lineas !== undefined ? recortar(conContexto(lineas, CONTEXTO_DEL_DIFF), TECHO_DEL_DIFF) : undefined;
  return (
    <Box flexDirection="column" borderStyle="round" borderColor={temaInk.aviso} paddingX={1}>
      <Text bold color={temaInk.aviso}>{`APROBACIÓN ${estado.indice + 1}/${pendientes.length}`}</Text>
      <Text>{`  ${p.descripcion}`}</Text>
      {fichero(p.id) !== undefined ? <Text>{`  fichero: ${fichero(p.id)}`}</Text> : null}
      <Text color={temaInk.mudo}>{`  quién: ${p.origen}`}</Text>
      {visibles?.lineas.map((l, i) => (
        <Text
          key={i}
          color={l.tipo === "anadido" ? temaInk.anadido : l.tipo === "quitado" ? temaInk.quitado : temaInk.mudo}
        >
          {`  ${l.tipo === "anadido" ? "+ " : l.tipo === "quitado" ? "- " : "  "}${l.texto}`}
        </Text>
      ))}
      {visibles !== undefined && visibles.recortadas > 0 ? (
        <Text color={temaInk.mudo}>{`  … y ${visibles.recortadas} líneas más`}</Text>
      ) : null}
      <Text bold>{"  ¿Aprobar? [S/n]"}</Text>
    </Box>
  );
}
```

Con `useInput` importado de ink junto a Box/Text (corregir el import). Los tests del componente: render con 1 pendiente, `stdin.write("s")` → `alTerminar` resuelve approve; `stdin.write("n")` → reject; Enter → reject.

- [ ] **Step 3: `npm test` → commit**

```bash
git add src/cli/tui/aprobarTui.tsx src/cli/tui/aprobarTui.test.tsx
git commit -m "TUI: modal de aprobación fail-closed con diff coloreado"
```

---

### Task 8: las costuras de `Consola` para la piel TUI

**Goal:** `Consola` gana dos costuras opcionales (`piel`, `aprobacionesTui`) y los ejecutores las usan si están — sin cambiar nada en el camino stdio.

**Files:**
- Modify: `src/cli/consola.ts` (interfaz `Consola`, `ejecutarTurnoGuionizado`)
- Modify: `src/cli/main.ts:218-273` (`crearEjecutorReal`)
- Test: `src/cli/consola.test.ts`, `src/cli/main.test.ts`

**Acceptance Criteria:**
- [ ] `ejecutarTurnoGuionizado` usa `consola.piel?.() ?? crearPielStdio(consola.escribir)`.
- [ ] `crearEjecutorReal` usa `consolaReal.piel?.()` para la piel del turno y `consolaReal.aprobacionesTui` para el puerto `pedirAprobacion` si están; si no, el camino de hoy.
- [ ] Sin las costuras presentes, TODOS los tests existentes siguen en verde (no cambia nada).

**Verify:** `npm test` → verde completo.

**Steps:**

- [ ] **Step 1: RED** — en `consola.test.ts`, un test: consola de pega con `piel: () => pielDePrueba` (una piel que apunta actos) corriendo una línea de prosa guionizada → los actos caen en esa piel y NO en `consola.escribir`.
- [ ] **Step 2: GREEN** — en `consola.ts`:

```typescript
export interface Consola {
  lineas: AsyncIterable<string>;
  escribir: Escribir;
  preguntar: Preguntar;
  interactivo: boolean;
  leerSecreto: (pregunta: string) => Promise<string>;
  /**
   * La piel que los turnos usan para pintarse. La consola stdio no la define (se usa
   * `crearPielStdio`); la TUI la aporta — mismo contrato `Piel`, otro render.
   */
  piel?: () => Piel;
  /**
   * Las aprobaciones propias (modal TUI). Ausente = `pedirDecisiones` por readline.
   * Mismo contrato que el puerto `pedirAprobacion` de `abrirSesionReal`.
   */
  aprobacionesTui?: (
    pendientes: PendienteDeAprobacion[],
    ficheros: Map<string, string>,
    diffs: Map<string, LineaDeDiff[]>
  ) => Promise<Map<string, Decision>>;
}
```

Con imports: `import type { Piel } from "../core/turno.js";`, `import type { PendienteDeAprobacion } from "../core/events.js";`, `import type { LineaDeDiff } from "../core/diff.js";`, y `import type { Decision } from "../vendor/hitl.js";` (el tipo que `aprobar.ts` ya usa para `pedirDecisiones`).

Y en `ejecutarTurnoGuionizado`: `const piel = consola.piel?.() ?? crearPielStdio(consola.escribir);`

- [ ] **Step 3: GREEN — `crearEjecutorReal` en `main.ts`**: sustituir la construcción de piel y el puerto:

```typescript
        pedirAprobacion: async (lista, ficheros, diffs) =>
          consolaReal.aprobacionesTui !== undefined
            ? consolaReal.aprobacionesTui(lista, ficheros, diffs)
            : pedirDecisiones(lista, consolaReal.preguntar, consolaReal.escribir, {
                interactive: consolaReal.interactivo,
                fichero: (id) => ficheros.get(id),
                diff: (id) => diffs.get(id),
              }),
```

Y la línea del turno: `await sesion.turno(peticion, consolaReal.piel?.() ?? crearPielStdio(consolaReal.escribir));`

- [ ] **Step 4: `npm test` completo → commit**

```bash
git add src/cli/consola.ts src/cli/consola.test.ts src/cli/main.ts
git commit -m "Consola: costuras piel y aprobacionesTui para la TUI"
```

---

### Task 9: montar la TUI — `correrConsolaTui` y banderas

**Goal:** `main.ts` decide piel; con TTY arranca la app Ink que implementa `Consola` y reusa `correrConsola`.

**Files:**
- Create: `src/cli/tui/app.tsx`
- Create: `src/cli/tui/correrTui.ts`
- Modify: `src/cli/main.ts` (banderas `--tui`/`--no-tui`, rama TUI en `entrarEnConsola`)
- Test: `src/cli/tui/correrTui.test.ts`

**Acceptance Criteria:**
- [ ] `App` compone layout: transcript (flexGrow) | sidebar (fija, ~30 col), entrada abajo, barra de estado al pie.
- [ ] `crearConsolaTui` devuelve una `Consola` completa: `lineas` cola async (lo que entra por `Entrada`), `escribir`→store, `interactivo: true`, `piel: () => crearPielTui(store)`, `leerSecreto` con entrada oculta, `aprobacionesTui` vía `pedirDecisionesTui`.
- [ ] `correrConsolaTui(fuentes, raiz, guion)` monta ink (`render`, `exitOnCtrlC: false`), corre `correrConsola(consolaTui, estado, ejecutar)` con el MISMO `crearEjecutorReal` de main.ts, y desmonta limpio al terminar.
- [ ] `--no-tui` fuerza stdio incluso con TTY; `--tui` fuerza la TUI; por omisión TTY → TUI. Sin TTY (pipe) sigue stdio.
- [ ] El e2e de pipe sigue byte-idéntico.

**Verify:** `npx vitest run src/cli/tui/correrTui.test.ts` → verde; `npm test` → verde.

**Steps:**

- [ ] **Step 1: RED** — test en `src/cli/tui/correrTui.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { crearConsolaTui } from "./correrTui.js";

describe("la consola TUI", () => {
  it("implementa Consola: lineas es la cola de lo enviado, escribir y piel comparten store", async () => {
    const { consola, enviar } = crearConsolaTui({ raiz: "/tmp/proyecto" });
    const leidas: string[] = [];
    const lector = (async () => {
      for await (const linea of consola.lineas) {
        leidas.push(linea);
        break;
      }
    })();
    enviar("hola equipo");
    await lector;
    expect(leidas).toEqual(["hola equipo"]);
    expect(consola.interactivo).toBe(true);

    // La piel y el `escribir` caen en el MISMO store: un turno y un comando se ven
    // en el mismo transcript.
    consola.escribir("·  hola");
    const piel = consola.piel!();
    piel.fase("planificando");
    piel.linea("→ lee app.xne");
    const actos = consola.actos();
    expect(actos.some((a) => a.tipo === "tool" && a.texto.includes("hola"))).toBe(true);
    expect(actos.some((a) => a.tipo === "fase")).toBe(true);

    // aprobar por defecto: fail-closed sin modal montado.
    await expect(
      consola.aprobacionesTui!([], new Map(), new Map())
    ).resolves.toEqual(new Map());
  });
});
```

- [ ] **Step 2: GREEN — `correrTui.ts`** — la consola y el montaje:

```typescript
/**
 * El montaje de la TUI: la Consola que la app Ink implementa y el arranque/desmontaje.
 * Todo lo demás (el lazo de comandos, el estado de sesión, el ejecutor) es EL MISMO de
 * `consola.ts` y `main.ts`: la TUI solo aporta piel, entrada y aprobaciones.
 */
import { render } from "ink";
import { createElement } from "react";
import { crearStore, type Acto } from "./store.js";
import { crearPielTui } from "./pielTui.js";
import { App } from "./app.js";
import { pedirDecisionesTui } from "./aprobarTui.js";
import { correrConsola, type Consola, type EstadoDeSesion } from "../consola.js";

export function crearConsolaTui({ raiz }: { raiz: string }): {
  consola: Consola;
  enviar: (linea: string) => void;
  actos: () => Acto[];
} {
  const store = crearStore();
  const cola: string[] = [];
  const esperando: ((r: IteratorResult<string>) => void)[] = [];
  const enviar = (linea: string): void => {
    const despertar = esperando.shift();
    if (despertar !== undefined) despertar({ value: linea, done: false });
    else cola.push(linea);
  };
  const consola: Consola = {
    interactivo: true,
    lineas: {
      [Symbol.asyncIterator]() {
        return {
          next: () =>
            cola.length > 0
              ? Promise.resolve({ value: cola.shift()!, done: false as const })
              : new Promise<IteratorResult<string>>((resolver) => esperando.push(resolver)),
        };
      },
    },
    escribir: (texto) => store.linea(texto, "sistema"),
    preguntar: async (pregunta) => {
      // v1: las preguntas de la consola (p. ej. el asistente de creación de proyecto)
      // entran por la misma Entrada, precedidas del enunciado en el transcript.
      store.linea(pregunta, "sistema");
      return new Promise((resolver) => esperandoPreguntas.push(resolver));
    },
    leerSecreto: async (pregunta) => {
      store.linea(pregunta, "sistema");
      return new Promise((resolver) => esperandoSecretos.push(resolver));
    },
    piel: () => crearPielTui(store),
    aprobacionesTui: (pendientes, _ficheros, _diffs) =>
      pedirDecisionesTui(pendientes, (lista, alTerminar) => {
        montarModal = { pendientes: lista, alTerminar }; // la app pinta el modal
        store.pausa();
      }),
  };
  // esperandoPreguntas/esperandoSecretos: colas gemelas de la de `enviar`; la Entrada
  // en modo pregunta/secreto las resuelve (ver `app.tsx`).
  return { consola, enviar, actos: () => store.estado().actos };
}

export async function correrConsolaTui(fuentes: string[], raiz: string, guion: string[]): Promise<void> {
  const { consola } = crearConsolaTui({ raiz });
  const estado: EstadoDeSesion = { hilo: [], raiz, fuentes };
  const instancia = render(createElement(App, { /* props del store y callbacks */ }), {
    exitOnCtrlC: false,
  });
  try {
    await correrConsola(consola, estado);
  } finally {
    instancia.unmount(); // Nunca un terminal roto: se desmonta también si revienta.
  }
}
```

(NOTA de ejecución: `esperandoPreguntas`, `esperandoSecretos` y `montarModal` son las colas/ranuras que `app.tsx` consume — se declaran junto a `cola` con el mismo patrón resolver-pendiente; el input en modo pregunta escribe en ellas. El `createElement(App, …)` recibe `store`, `enviar`, `completa` (el completer puro), `historial` y `datosSidebar` de la sesión.)

- [ ] **Step 3: GREEN — `app.tsx`** (layout, ~80 líneas):

```tsx
/** La raíz Ink: dos columnas, entrada abajo, barra al pie. */
import { Box, Text, Static } from "ink";
import { useEffect, useState } from "react";
import type { ReactNode } from "react";
import { crearStore } from "./store.js";
import { Transcript } from "./transcript.js";
import { Entrada } from "./entrada.js";
import { Sidebar } from "./sidebar.js";
import { temaInk } from "./temaInk.js";
import type { EstadoDeTui } from "./store.js";

export function App({
  store,
  filas,
  alEnviar,
  completa,
  historial,
  ocupado,
  datosSidebar,
  modeloActivo,
  proyecto,
}: {
  store: ReturnType<typeof crearStore>;
  filas: number;
  alEnviar: (l: string) => void;
  completa: (l: string) => [string[], string];
  historial: readonly string[];
  ocupado: boolean;
  datosSidebar: Parameters<typeof Sidebar>[0];
  modeloActivo: string;
  proyecto: string;
}): ReactNode {
  const [estado, setEstado] = useState<EstadoDeTui>(store.estado());
  useEffect(() => {
    store.suscribir(() => setEstado({ ...store.estado() }));
  }, [store]);
  const alturaTranscript = Math.max(5, filas - 4);
  return (
    <Box flexDirection="row" width="100%">
      <Box flexDirection="column" flexGrow={1}>
        <Transcript store={store} altura={alturaTranscript} />
        <Entrada alEnviar={alEnviar} completa={completa} ocupado={ocupado} historial={historial} />
        <Text color={temaInk.mudo}>{`${modeloActivo} · ${proyecto} · /ayuda`}</Text>
      </Box>
      <Box width={30} paddingLeft={1}>
        <Sidebar {...datosSidebar} />
      </Box>
    </Box>
  );
}
```

- [ ] **Step 3: GREEN — `correrTui.ts`**: la `Consola` TUI + el montaje (la cola `lineas` con resolvers pendientes como en el esbozo del plan; `historial` de líneas enviadas alimentando `Entrada`; `leerSecreto` con un input sin eco dentro de la TUI). El fichero reusa `correrConsola`, `EstadoDeSesion` y `COMANDOS` de `consola.ts` y el completer `crearCompleter` con `() => ficherosDelProyecto(raiz)`.
- [ ] **Step 4: GREEN — `main.ts`**: en la rama de consola (~543) — banderas y rama TUI:

```typescript
  const pedirTui = argv.includes("--tui");
  const sinTui = argv.includes("--no-tui");
  const usarTui = sinTui ? false : process.stdout.isTTY === true || pedirTui;
```

Y en `entrarEnConsola`, al llegar al lazo: `if (usarTui) return correrConsolaTui(fuentes, raiz, guion);` (con la costura de test `usarTui` como parámetro opcional `= usarTuiPorOmision()`, para que los tests existan sin TTY).
- [ ] **Step 5: `npm run typecheck && npm test` → commit**

```bash
git add src/cli/tui/app.tsx src/cli/tui/correrTui.ts src/cli/tui/correrTui.test.ts src/cli/main.ts
git commit -m "TUI: montaje Ink, consola TUI y banderas --tui/--no-tui"
```

---

### Task 10: verificación final y documentación

**Goal:** El invariante completo: suite verde sin red, e2e byte-idéntico, sesión real contra Ollama, docs al día.

**Files:**
- Modify: `CLAUDE.md`, `docs/COMO-PROBARLO.md`, `README.md`

**Acceptance Criteria:**
- [ ] `npm run typecheck && npm test` verde completo.
- [ ] `printf 'hola\n/salir\n' | ./bin/xonecode --guion` byte-idéntico a la salida previa (comparar contra el worktree de HEAD si hiciera falta).
- [ ] Sesión TUI real contra Ollama (`--tui`): turno de prosa con fases y duración, aprobación modal con diff, `/modelo` en caliente, `/salir` desmonta limpio y devuelve a la shell.
- [ ] `--no-tui` con TTY cae al stdio de siempre.
- [ ] Docs: CLAUDE.md (frontera ink bajo `cli/tui/` con el guardián, modos TUI/stdio y banderas), COMO-PROBARLO (cómo probar la TUI sin gastar: `--guion --tui`), README (mención en «Cómo está construido»).

**Verify:** `npm test` + e2e + sesión real.

**Steps:**

- [ ] **Step 1: Suite + e2e byte-idéntico** (el comando de arriba; guardar salida y `diff`).
- [ ] **Step 2: Sesión real** — `./bin/xonecode --guion --tui` para el camino barato primero, luego `./bin/xonecode --tui` contra Ollama en un proyecto XOne real.
- [ ] **Step 3: Docs** — editar los tres ficheros con lo mínimo que no mienta.
- [ ] **Step 4: Commit final**

```bash
git add CLAUDE.md docs/COMO-PROBARLO.md README.md
git commit -m "TUI: verificación final y documentación de modos"
```